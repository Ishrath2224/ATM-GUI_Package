# ATM-GUI_Package
This is an ATM portal designed using Java GUI package. The portal needs a valid pin to login. the main menu which displays after login has withdrawl, deposit, chaeck balance and many more buttons. Each button directs to a different frame doing the specific task. Every frame has a back and exit option along with print receipt option for some frames.
